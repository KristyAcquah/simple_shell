#include <stdlib.h>
#include <stdio.h>

/**
 * add_number -  adds two integer a and b
 * @a: first integer
 * @b: second integer
 *
 * Return: the sum of the integer a and b
 */
int add_number(int a, int b)
{

	int sum = a + b;

	printf("%d", sum);

	return (sum);
}
